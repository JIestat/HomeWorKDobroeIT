
public class Util {

    public static String nameObjects(Object object){// получаю на вход обьект
        String nameClass = String.valueOf(object.getClass());// получаю название класса, конветирую в строку, записываю в переменную стринг.
        return nameClass; //метод взвращает строку с классом обьекта
    }
}
